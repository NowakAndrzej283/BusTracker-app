import {View, Text, StyleSheet, FlatList} from 'react-native';


export default function TransportTypeBar({navigation}){
    return (
        <View style={styles.rootContainer}>
            
        </View>
    );
}

const styles = StyleSheet.create({
    rootContainer: {
        flex: 1,
    }
})